{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://claude-team/schemas/escalation.json",
  "title": "EscalationPayload",
  "description": "Message payload for escalating issues to PM or PL",
  "type": "object",
  "required": ["taskId", "severity", "reason", "context"],
  "properties": {
    "taskId": {
      "type": "string",
      "description": "Unique identifier for the task being escalated"
    },
    "severity": {
      "type": "string",
      "description": "Severity level of the issue",
      "enum": ["low", "medium", "high", "critical"]
    },
    "reason": {
      "type": "string",
      "description": "Detailed explanation of why escalation is needed"
    },
    "suggestedAction": {
      "type": "string",
      "description": "Optional suggestion on how to resolve the issue"
    },
    "context": {
      "type": "object",
      "description": "Additional context data relevant to the escalation",
      "additionalProperties": true
    }
  },
  "additionalProperties": false
}
